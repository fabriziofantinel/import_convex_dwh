# Webhook Server Configuration
# Copy this file to .env and update with your values

# Security: Secret token for webhook authentication
# Generate a strong random token: python -c "import secrets; print(secrets.token_urlsafe(32))"
WEBHOOK_TOKEN=change-this-secret-token

# Convex callback URL (HTTP action endpoint)
# This is where the webhook server sends sync results
# Format: https://your-deployment.convex.site/api/webhook
CONVEX_WEBHOOK_URL=

# Python executable path
# Windows example: C:\Users\YourName\AppData\Local\Programs\Python\Python311\python.exe
# Linux example: /usr/bin/python3
PYTHON_EXE=C:\Users\Fabrizio Fantinel\AppData\Local\Programs\Python\Python311\python.exe

# Sync script path (relative to webhook_server.py)
SYNC_SCRIPT_PATH=sync.py

# Server host and port
# Use 0.0.0.0 to accept connections from any IP
# Use 127.0.0.1 for localhost only
HOST=0.0.0.0
PORT=5000
